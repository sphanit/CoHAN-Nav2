name: Trigger Read the Docs

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'

jobs:
  trigger-rtd:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger RTD
        run: echo "The Workflow is complete! Docs will be rebuilt."
