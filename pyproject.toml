[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "CoHAN-Nav2"
authors = [{name = "Phani Teja Singamaneni", email = "phani-teja.Singamaneni@inria.fr"}]
dynamic = ["version", "description"]
